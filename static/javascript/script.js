shortify = () =>{
    list = document.getElementsByClassName("preview-text");
    console.log(list)
    // para.innerText = para.innerText.substr(0, 100) + '...';
    var i;
    for (i = 0; i < list.length; i++) {
        console.log("hello")
    }
}

shortify();